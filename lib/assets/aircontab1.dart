import 'package:flutter/material.dart';


class airconTab1 extends StatelessWidget {
  const airconTab1({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('aircon tab 1')),
    );
  }
}
