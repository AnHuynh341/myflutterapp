import 'package:flutter/material.dart';


class airconTab2 extends StatelessWidget {
  const airconTab2({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('aircon tab 2')),
    );
  }
}
