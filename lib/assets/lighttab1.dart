import 'package:flutter/material.dart';


class lightTab1 extends StatelessWidget {
  const lightTab1({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('light tab 1')),
    );
  }
}
