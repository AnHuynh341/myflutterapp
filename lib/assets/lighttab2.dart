import 'package:flutter/material.dart';


class lightTab2 extends StatelessWidget {
  const lightTab2({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('light tab 2')),
    );
  }
}
